دستور نهایی بازطراحی ریپوزیتوری GynQB

نقش و هدف

تو داخل یک ریپوزیتوری Git کلون‌شده روی سرور کار می‌کنی و باید آن را به نسخه نهایی، ساده و قابل انتشار بانک سؤالات زنان و زایمان تبدیل کنی.

مسیر ریپوزیتوری:

/home/hadi/gyn/hadi/gynqb

ریپوزیتوری اصلی:

https://github.com/hadiebiii/gynqb

پروژه قبلاً فایل‌ها، اسکریپت‌ها، خروجی‌های آزمایشی و ساختارهای موقت داشته است. اکنون باید تمام اجزای غیرضروری را پس از بررسی حذف کنی و فقط نسخه نهایی سایت، داده نهایی و فایل‌های مستندات ضروری را نگه داری.

هدف نهایی:

نمایش کامل و صحیح ۱٬۶۰۸ سؤال یکتای نهایی.

ایجاد یک وب‌سایت فارسی، RTL، responsive و مناسب موبایل و دسکتاپ.

ایجاد یک پنل مدیریت سمت مرورگر برای مشاهده، فیلتر، ویرایش و خروجی‌گرفتن از سؤالات نیازمند اصلاح.

حذف اسکریپت‌های قدیمی، SQLite، فایل‌های آزمایشی، خروجی‌های موقت و پوشه‌های غیرضروری.

باقی‌ماندن یک ساختار بسیار ساده و قابل نگهداری.

عدم ایجاد backend، database server یا سیستم پیچیده.

حفظ کامل فایل‌های نهایی JSONL داخل پوشه final.

۱. محدودیت مهم معماری

وب‌سایت باید static باشد و روی GitHub Pages اجرا شود.

GitHub Pages اجازه نمی‌دهد پنل مدیریت، فایل‌های موجود روی سرور یا ریپوزیتوری را مستقیماً ویرایش و ذخیره کند. بنابراین پنل مدیریت باید به این شکل کار کند:

داده‌ها را از questions.json بخواند.

ویرایش‌ها را موقتاً در localStorage ذخیره کند.

امکان import فایل JSON یا JSONL را بدهد.

امکان export نسخه اصلاح‌شده به دو فرمت JSON و JSONL را بدهد.

در مرورگرهای پشتیبانی‌شده، می‌تواند با File System Access API فایل انتخاب‌شده توسط کاربر را ذخیره کند.

اگر این API در دسترس نبود، فایل اصلاح‌شده را به‌صورت download تحویل دهد.

هیچ ادعای نادرستی درباره ذخیره مستقیم روی GitHub یا سرور نداشته باشد.

برای اعمال دائمی اصلاحات، فایل خروجی پنل باید جایگزین فایل مربوط در ریپوزیتوری شود و سپس commit و push انجام شود.

ایجاد موارد زیر ممنوع است:

Flask
Django
FastAPI
Streamlit
Node server
Backend API
SQLite در مرورگر
Database server
React
Vue
Angular
Build pipeline
Python script دائمی
Node script دائمی

موارد مجاز:

HTML
CSS
Vanilla JavaScript
JSON
JSONL منبع
localStorage
File System Access API با fallback دانلود
GitHub Pages

استفاده از دستورات موقت shell یا Python یک‌خطی برای بررسی و تبدیل داده در زمان اجرای مأموریت مجاز است؛ اما در پایان نباید فایل اسکریپت build یا export دائمی در ریپوزیتوری باقی بماند.

۲. بررسی اولیه اجباری

ابتدا وارد مسیر پروژه شو:

cd /home/hadi/gyn/hadi/gynqb

سپس بدون ایجاد تغییر، وضعیت کامل را بررسی کن:

pwd
ls -lah
find . -maxdepth 4 -type f ! -path './.git/*' | sort
git status --short
git branch --show-current
git remote -v
git log --oneline -10

پوشه داده نهایی را بررسی کن:

find final -maxdepth 2 -type f -print | sort

چند خط اول هر فایل JSONL را با روش امن بررسی کن و schema واقعی را مشخص کن.

قبل از حذف هر فایل، وابستگی‌های آن را جست‌وجو کن:

grep -R "نام-فایل" -n . --exclude-dir=.git

قواعد ایمنی Git:

از git reset --hard استفاده نکن.

از git clean -fd استفاده نکن.

تغییرات commitنشده قبلی کاربر را حذف نکن.

فایل ناشناخته را بدون بررسی حذف نکن.

تاریخچه Git را بازنویسی نکن.

force push انجام نده.

اگر docs/CNAME وجود دارد، آن را حفظ کن.

اگر working tree از ابتدا dirty است، تغییرات قبلی را از تغییرات خودت تفکیک کن.

۳. منبع نهایی داده

بانک جامع و نهایی سؤالات داخل پوشه زیر قرار دارد:

final/

محتوای نهایی باید شامل دقیقاً ۱٬۶۰۸ سؤال یکتا باشد.

هر رکورد JSONL دارای فیلدهای اصلی زیر است:

id
stem
options
correct_option
explanation
exam_type
exam_year
reference
chapter
occurrences
quality

تعریف فیلدها:

فیلد

مفهوم

id

شناسه یکتای سؤال

stem

متن اصلی سؤال

options

گزینه‌های سؤال

correct_option

پاسخ صحیح تأییدشده

explanation

پاسخ تشریحی بالینی

exam_type

بورد، ارتقا، فلوشیپ یا نوع آزمون

exam_year

سال برگزاری

reference

کتاب یا مرجع اصلی

chapter

فصل دقیق مرجع

occurrences

آدرس‌های مبدأ و شناسه‌های قبلی سؤال

quality

complete یا needs_review

هیچ داده پزشکی، پاسخ، رفرنس یا فصل را از خودت تولید نکن.

اگر داده‌ای ناقص است:

رکورد را حذف نکن.

مقدار موجود را حفظ کن.

آن را با quality: "needs_review" مشخص کن.

در پنل مدیریت قابل فیلتر و اصلاح باشد.

۴. اعتبارسنجی داده منبع

تمام فایل‌های *.jsonl داخل پوشه final را بخوان و بررسی کن.

الزامات:

هر خط باید JSON معتبر باشد.

تعداد نهایی رکوردهای یکتا بر اساس id باید ۱٬۶۰۸ باشد.

شناسه خالی یا تکراری نباید بدون گزارش باقی بماند.

stem نباید خالی باشد.

options باید در صورت امکان چهار گزینه داشته باشد.

correct_option باید با یکی از گزینه‌ها تطبیق داشته باشد.

quality فقط یکی از این دو مقدار باشد:

complete
needs_review

occurrences باید بدون حذف اطلاعات اصلی حفظ شود.

ترتیب و محتوای علمی رکوردها نباید به‌صورت حدسی تغییر کند.

متن‌های فارسی فقط از نظر فاصله، کاراکترهای نامرئی و اشکالات نمایشی واضح normalize شوند؛ محتوای پزشکی بازنویسی نشود.

اگر چند فایل JSONL وجود دارد:

منبع اصلی هر رکورد را مشخص کن.

رکوردهای دارای id یکسان را کورکورانه ادغام نکن.

اختلاف‌ها را گزارش کن.

نسخه‌ای را انتخاب کن که در پوشه final به‌عنوان خروجی نهایی معرفی شده یا کامل‌تر است.

در صورت عدم قطعیت، اطلاعات را حذف نکن و سؤال را needs_review نگه دار.

۵. نرمال‌سازی ایمن داده

شناسه

id باید:

رشته باشد.

خالی نباشد.

در کل بانک یکتا باشد.

بدون تغییر غیرضروری حفظ شود.

متن سؤال

برای stem فقط این پاکسازی‌ها مجاز است:

حذف فاصله‌های تکراری.

حذف کاراکترهای کنترلی و نامرئی.

یکسان‌سازی line ending.

اصلاح فاصله قبل و بعد علائم نگارشی در موارد کاملاً واضح.

حفظ نیم‌فاصله‌های درست فارسی.

حفظ اصطلاحات انگلیسی و اختصارات پزشکی.

بازنویسی علمی یا ادبی متن ممنوع است.

گزینه‌ها

options ممکن است object یا array باشد. آن را در خروجی عمومی به ساختار ثابت زیر تبدیل کن:

{
  "الف": "متن گزینه اول",
  "ب": "متن گزینه دوم",
  "ج": "متن گزینه سوم",
  "د": "متن گزینه چهارم"
}

اگر ساختار اصلی array است، ترتیب را حفظ کن.

اگر کمتر از چهار گزینه وجود دارد:

گزینه جعلی نساز.

مقدار خالی یا گزینه‌های موجود را حفظ کن.

quality را needs_review قرار بده.

پاسخ صحیح

correct_option را در صورت امکان به یکی از موارد زیر normalize کن:

الف
ب
ج
د

ورودی‌های قابل قبول:

A
B
C
D
a
b
c
d
1
2
3
4
۱
۲
۳
۴
option_a
option_b
option_c
option_d
گزینه الف
گزینه ب
گزینه ج
گزینه د

اگر پاسخ قابل تشخیص نیست:

"correct_option": ""

و:

"quality": "needs_review"

پاسخ را حدس نزن.

پاسخ تشریحی

explanation باید:

متن موجود را حفظ کند.

از HTML و script ناامن پاک باشد.

در صورت خالی بودن باعث crash نشود.

از خود مدل تکمیل نشود.

نوع آزمون و سال

مقدارهای اصلی را حفظ کن. فقط نمایش فارسی و مرتب در UI انجام شود.

رفرنس و فصل

نام مرجع و فصل موجود را حفظ کن.

فصل یا رفرنس جدید از روی حدس تولید نکن.

مقادیر خالی با برچسب نمایشی «نامشخص» نشان داده شوند.

مقدار اصلی در داده از بین نرود.

۶. ساختار نهایی ریپوزیتوری

پس از بررسی و حذف موارد غیرضروری، ساختار هدف باید تا حد امکان به این شکل باشد:

gynqb/
├── .git/
├── final/
│   └── [فایل‌ها یا فایل نهایی JSONL]
├── docs/
│   ├── index.html
│   ├── admin.html
│   ├── questions.json
│   └── CNAME              # فقط اگر از قبل وجود دارد
├── .gitignore
├── README.md
└── prompt.md

فایل‌های زیر فقط در صورت وجود نیاز واقعی نگه داشته شوند:

LICENSE

پوشه‌ها و فایل‌های زیر پس از بررسی وابستگی و در صورت غیرضروری بودن حذف شوند:

scripts/
exports/
tests/ قدیمی
data/ قدیمی
docs/data/
docs/data-loader-example.js
فایل‌های SQLite
فایل‌های database
فایل‌های debug
فایل‌های log
فایل‌های cache
خروجی‌های آزمایشی
promptهای قدیمی
نسخه‌های تکراری JSON
نسخه‌های تکراری HTML
فایل‌های موقت

فایل prompt.md جدید باید در ریشه ریپوزیتوری باقی بماند.

اگر فایل یا پوشه‌ای برای عملکرد نسخه نهایی لازم است، آن را حذف نکن و دلیل حفظ آن را در گزارش نهایی توضیح بده.

۷. تولید فایل عمومی questions.json

بدون ایجاد اسکریپت دائمی، داده‌های نهایی JSONL را یک‌بار به فایل زیر تبدیل کن:

docs/questions.json

ساختار فایل ترجیحاً به این شکل باشد:

{
  "meta": {
    "title": "بانک سؤالات زنان و زایمان",
    "version": 1,
    "total_questions": 1608,
    "generated_at": "ISO-8601 timestamp",
    "source": "final JSONL",
    "schema_version": "1.0"
  },
  "questions": [
    {
      "id": "شناسه یکتا",
      "stem": "متن سؤال",
      "options": {
        "الف": "...",
        "ب": "...",
        "ج": "...",
        "د": "..."
      },
      "correct_option": "الف",
      "explanation": "...",
      "exam_type": "...",
      "exam_year": "...",
      "reference": "...",
      "chapter": "...",
      "occurrences": [],
      "quality": "complete"
    }
  ]
}

قواعد:

خروجی باید UTF-8 معتبر باشد.

تمام ۱٬۶۰۸ سؤال حفظ شوند.

هیچ فیلد اصلی حذف نشود.

occurrences در خروجی عمومی قابل مشاهده باشد؛ اما در UI می‌تواند داخل بخش جمع‌شونده نمایش داده شود.

هیچ log، token، secret، مسیر خصوصی سیستم، prompt داخلی یا داده غیرمرتبط وارد JSON نشود.

JSON باید با python3 -m json.tool معتبر باشد.

فایل را minify نکن اگر خوانایی و diff بهتر با indent حفظ می‌شود.

ترتیب سؤال‌ها پایدار و قابل پیش‌بینی باشد؛ ترجیحاً ترتیب فایل نهایی یا ترتیب شناسه‌ها.

۸. طراحی وب‌سایت اصلی

فایل اصلی:

docs/index.html

این فایل باید:

یک HTML کامل باشد.

CSS و JavaScript را داخل خود فایل داشته باشد.

به فایل‌های JS و CSS buildشده یا پوشه assets نیاز نداشته باشد.

داده را با مسیر نسبی زیر بخواند:

fetch('./questions.json')

استفاده از مسیر absolute زیر ممنوع است:

fetch('/questions.json')

HTML:

<html lang="fa" dir="rtl">

عنوان:

بانک سؤالات زنان و زایمان

زیرعنوان:

مجموعه جامع سؤالات بورد، ارتقا و فلوشیپ

صفحه اصلی

در بالای سایت آمار زیر نمایش داده شود:

تعداد کل سؤال‌ها.

تعداد سؤال‌های کامل.

تعداد سؤال‌های نیازمند بازبینی.

تعداد رفرنس‌ها.

تعداد فصل‌ها.

تعداد سال‌های آزمون.

فیلترها

کاربر بتواند بر اساس این موارد فیلتر کند:

جستجو در متن سؤال.

نوع آزمون.

سال آزمون.

رفرنس.

فصل.

وضعیت کیفیت.

پاسخ‌داده‌شده یا پاسخ‌داده‌نشده.

پاسخ صحیح یا غلط کاربر.

سؤال‌های نشان‌شده به‌عنوان علاقه‌مندی.

فیلترها باید قابل پاک‌کردن باشند.

نمایش سؤال

هر سؤال باید تمام اطلاعات زیر را نمایش دهد:

شناسه سؤال
نوع آزمون
سال آزمون
رفرنس
فصل
وضعیت کیفیت
صورت سؤال
چهار گزینه
پاسخ صحیح پس از ثبت پاسخ
پاسخ تشریحی
occurrences یا منابع مبدأ

occurrences در یک بخش جمع‌شونده با عنوان زیر نمایش داده شود:

منابع و محل‌های مشاهده سؤال

رفتار پاسخ‌دهی

قبل از انتخاب گزینه:

پاسخ صحیح و توضیح پنهان باشد.

پس از انتخاب:

گزینه انتخاب‌شده مشخص شود.

گزینه صحیح مشخص شود.

درست یا غلط بودن پاسخ کاربر اعلام شود.

پاسخ تشریحی نمایش داده شود.

رفرنس و فصل تکرار شود.

پاسخ در localStorage ذخیره شود.

اگر پاسخ صحیح مشخص نیست:

پاسخ صحیح این سؤال هنوز نیازمند بازبینی است.

در این حالت سؤال در آمار درست یا غلط محاسبه نشود.

ناوبری

کنترل‌های زیر وجود داشته باشند:

سؤال قبلی
سؤال X از Y
ورودی رفتن به سؤال
سؤال بعدی

در موبایل نیز قابل استفاده باشند.

پیشرفت کاربر

در localStorage با یک کلید versioned ذخیره کن:

gynqb-user-progress-v1

موارد ذخیره‌شده:

پاسخ انتخاب‌شده.

درست یا غلط بودن.

علاقه‌مندی.

آخرین سؤال بازشده.

فیلترهای اخیر در صورت مناسب بودن.

یک دکمه برای پاک کردن پیشرفت کاربر با confirm وجود داشته باشد.

۹. طراحی پنل مدیریت

فایل:

docs/admin.html

این پنل backend ندارد و نباید امنیت جعلی ایجاد کند.

در بالای صفحه یک هشدار روشن نمایش بده:

این پنل به‌صورت محلی در مرورگر کار می‌کند. تغییرات تا زمان خروجی‌گرفتن و جایگزینی فایل در ریپوزیتوری، روی وب‌سایت عمومی ذخیره نمی‌شوند.

قابلیت‌های پنل

بارگذاری داده

پنل باید بتواند:

به‌صورت پیش‌فرض questions.json را بارگذاری کند.

فایل JSON را از سیستم کاربر import کند.

فایل JSONL را از سیستم کاربر import کند.

خطاهای parse را با شماره خط و پیام قابل فهم نمایش دهد.

فهرست سؤالات

نمایش جدولی یا فهرستی با ستون‌های زیر:

شناسه
خلاصه سؤال
نوع آزمون
سال
رفرنس
فصل
کیفیت
وضعیت ویرایش

فیلترهای مدیریت

حداقل فیلترها:

quality = needs_review

پاسخ صحیح خالی.

کمتر از چهار گزینه.

توضیح خالی.

رفرنس خالی.

فصل خالی.

نوع آزمون.

سال.

رفرنس.

فصل.

جستجو بر اساس شناسه.

جستجو در متن سؤال.

فقط رکوردهای ویرایش‌شده.

فیلتر پیش‌فرض پنل:

needs_review

فرم ویرایش

تمام فیلدهای زیر قابل ویرایش باشند:

id
stem
option الف
option ب
option ج
option د
correct_option
explanation
exam_type
exam_year
reference
chapter
occurrences
quality

قواعد:

تغییر id هشدار جدی نمایش دهد.

شناسه تکراری قابل ذخیره نباشد.

پاسخ صحیح باید با گزینه موجود تطبیق داشته باشد.

امکان تغییر quality از needs_review به complete وجود داشته باشد.

برای occurrences ویرایش JSON امن یا رابط list-based فراهم شود.

فرم دارای دکمه‌های ذخیره، لغو، بازگردانی رکورد و رفتن به سؤال بعدی نیازمند بازبینی باشد.

تغییرات ذخیره‌نشده هنگام خروج هشدار داده شوند.

ذخیره موقت

ویرایش‌ها در کلید جداگانه ذخیره شوند:

gynqb-admin-draft-v1

پنل باید پس از refresh بتواند draft را بازیابی کند.

دکمه‌ها:

ذخیره پیش‌نویس
بازیابی پیش‌نویس
حذف پیش‌نویس

خروجی

پنل باید خروجی‌های زیر را تولید کند:

gynqb-questions-updated.json
gynqb-questions-updated.jsonl

همچنین یک فایل تغییرات اختیاری:

gynqb-changes.json

ساختار فایل تغییرات:

{
  "exported_at": "...",
  "changed_count": 10,
  "changes": [
    {
      "id": "...",
      "changed_fields": ["chapter", "quality"]
    }
  ]
}

ذخیره با File System Access API

اگر مرورگر پشتیبانی می‌کند:

دکمه «ذخیره در فایل انتخاب‌شده» ارائه بده.

کاربر باید خودش فایل را انتخاب و مجوز بدهد.

بدون مجوز کاربر هیچ فایل محلی نوشته نشود.

fallback:

دانلود فایل با Blob و URL موقت.

آمار پنل

نمایش بده:

تعداد کل.

complete.

needs_review.

پاسخ صحیح خالی.

گزینه ناقص.

توضیح خالی.

رکوردهای ویرایش‌شده.

رکوردهای آماده خروجی.

۱۰. کیفیت نگارش فارسی

تمام متن‌های رابط کاربری را از نظر موارد زیر بررسی کن:

املای درست فارسی.

استفاده درست از «سؤال» به‌جای شکل‌های نادرست.

استفاده درست از «پاسخ‌نامه» یا «پاسخ تشریحی» به‌صورت یکنواخت.

استفاده درست از نیم‌فاصله.

عدم وجود متن placeholder انگلیسی بی‌دلیل.

عدم وجود پیام خطای فنی خام.

عدم وجود ترجمه ماشینی نامأنوس.

هماهنگی واژه‌ها در سایت اصلی و پنل مدیریت.

واژگان پیشنهادی ثابت:

سؤال
پاسخ صحیح
پاسخ تشریحی
نوع آزمون
سال آزمون
مرجع
فصل
نیازمند بازبینی
کامل
منابع مبدأ
پیشرفت
علاقه‌مندی
ویرایش
خروجی‌گرفتن

داده پزشکی را به بهانه اصلاح نگارشی بازنویسی نکن.

۱۱. طراحی بصری

ظاهر باید:

فارسی
RTL کامل
علمی
حرفه‌ای
ساده
بدون شلوغی
mobile-first
responsive
مناسب مطالعه طولانی

فونت اصلی:

Vazirmatn

fallback:

font-family: "Vazirmatn", Tahoma, Arial, sans-serif;

از Design Token استفاده کن:

:root {
  --c-page: #f2f4f7;
  --c-surface: #ffffff;
  --c-text: #1a1d23;
  --c-muted: #687080;
  --c-border: #dfe3e8;
  --c-primary: #1b2a4a;
  --c-blue: #2b5ba8;
  --c-blue-bg: #ebf1fb;
  --c-green: #1a7a4a;
  --c-green-bg: #ebf7f1;
  --c-red: #b22222;
  --c-red-bg: #fdeded;
  --c-warning: #9a6700;
  --c-warning-bg: #fff8ec;
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --shadow-card: 0 2px 12px rgba(27, 42, 74, 0.09);
}

قواعد:

از رنگ به‌تنهایی برای بیان درست، غلط یا هشدار استفاده نکن.

متن و آیکون نیز نمایش بده.

انیمیشن‌ها بسیار سبک باشند.

prefers-reduced-motion رعایت شود.

layout در عرض‌های ۳۲۰ پیکسل به بالا نشکند.

فرم پنل در موبایل قابل استفاده باشد.

۱۲. دسترس‌پذیری

الزامات:

lang="fa"

dir="rtl"

ساختار semantic.

heading hierarchy صحیح.

label واقعی برای inputها.

focus state واضح.

استفاده کامل با صفحه‌کلید.

aria-live برای پیام پاسخ، خطا و ذخیره.

اندازه مناسب دکمه‌ها برای لمس.

کنتراست مناسب.

عدم استفاده از outline: none بدون جایگزین.

دکمه‌های گزینه از عنصر button ساخته شوند.

modalها focus management داشته باشند.

بخش‌های جمع‌شونده از details/summary یا ARIA صحیح استفاده کنند.

۱۳. امنیت سمت مرورگر

داده JSON نباید مستقیماً و بدون escape وارد innerHTML شود.

ترجیح:

element.textContent = value;

اگر تولید HTML لازم است، تابع escape امن استفاده کن.

الزامات:

جلوگیری از اجرای <script> موجود در داده.

جلوگیری از DOM XSS.

عدم استفاده از eval.

عدم استفاده از new Function.

عدم ذخیره secret یا password.

عدم قراردادن token گیت‌هاب در کد.

عدم ادعای محافظت واقعی admin.html با password سمت JavaScript.

اگر برای ورود به پنل یک کد نمایشی اضافه می‌کنی، صریحاً بنویس که امنیت واقعی ایجاد نمی‌کند. ترجیحاً پنل بدون ورود جعلی ساخته شود و فقط URL آن جدا باشد.

۱۴. مدیریت خطا

اگر questions.json بارگذاری نشد:

خطا در بارگذاری بانک سؤال.
لطفاً فایل داده یا اتصال را بررسی کنید.

دکمه:

تلاش مجدد

اگر JSON یا JSONL واردشده خراب است:

نام فایل.

شماره خط در JSONL.

خلاصه خطا.

تعداد رکوردهای خوانده‌شده قبل از خطا.

نمایش stack trace خام در UI ممنوع است.

اگر localStorage خراب است:

برنامه crash نکند.

داده خراب کنار گذاشته شود.

پیام قابل فهم نمایش داده شود.

۱۵. README نهایی

README.md را کامل و کوتاه نگه دار.

حداقل بخش‌ها:

معرفی پروژه.

آدرس نسخه آنلاین.

تعداد سؤال‌ها.

ساختار نهایی.

توضیح فایل‌های final.

توضیح docs/questions.json.

نحوه تست محلی.

نحوه فعال‌سازی GitHub Pages.

نحوه استفاده از پنل مدیریت.

نحوه اعمال دائمی تغییرات پنل.

محدودیت static بودن پنل.

ملاحظات امنیتی.

روش جایگزینی JSON و JSONL اصلاح‌شده.

تست محلی:

python3 -m http.server 8000 --directory docs

آدرس:

http://localhost:8000/

پنل:

http://localhost:8000/admin.html

توضیح بده که اجرای مستقیم با file:// ممکن است به دلیل محدودیت fetch کار نکند.

۱۶. .gitignore

حداقل این موارد را اضافه یا حفظ کن:

*.sqlite
*.sqlite3
*.db

.env
.env.*
*.pem
*.key
cookies.txt

__pycache__/
*.pyc
.venv/
venv/
node_modules/

*.log
*.tmp
*.temp

.DS_Store
Thumbs.db

فایل‌های نهایی داخل final/ و docs/questions.json نباید ignore شوند.

۱۷. تست‌های نهایی داده

بدون ایجاد اسکریپت دائمی، اعتبارسنجی را اجرا کن.

اعتبار JSON

python3 -m json.tool docs/questions.json > /dev/null

تعداد و شناسه‌های یکتا

یک دستور موقت Python اجرا کن که این موارد را assert کند:

total_questions == 1608
len(questions) == 1608
تمام idها غیرخالی
تمام idها یکتا
تمام stemها غیرخالی
quality فقط complete یا needs_review
correct_option فقط خالی یا الف، ب، ج، د
options از نوع object

آمار کیفیت

گزارش کن:

تعداد کل.

complete.

needs_review.

پاسخ صحیح خالی.

کمتر از چهار گزینه.

explanation خالی.

reference خالی.

chapter خالی.

exam_type خالی.

exam_year خالی.

هیچ رکوردی را فقط برای عبور از تست حذف نکن.

۱۸. تست سایت محلی

سرور موقت:

python3 -m http.server 8000 --directory docs

بررسی کن:

/
admin.html
questions.json

با curl یا ابزار مرورگر تأیید کن که status موفق دریافت می‌شود.

بررسی JavaScript:

syntax error وجود نداشته باشد.

همه selectorها معتبر باشند.

بارگذاری ۱٬۶۰۸ سؤال انجام شود.

فیلترها کار کنند.

انتخاب پاسخ کار کند.

پاسخ صحیح پس از انتخاب نمایش داده شود.

سؤال بدون پاسخ صحیح crash نکند.

occurrences نمایش داده شود.

progress پس از refresh باقی بماند.

پنل رکوردهای needs_review را فیلتر کند.

ویرایش رکورد کار کند.

draft پس از refresh باقی بماند.

export JSON معتبر باشد.

export JSONL دارای یک JSON معتبر در هر خط باشد.

import JSON و JSONL کار کند.

duplicate id در پنل خطا بدهد.

متن HTML مخرب از داده اجرا نشود.

اگر Playwright یا مرورگر موجود است، smoke test انجام بده؛ اما dependency جدید دائمی اضافه نکن.

۱۹. حذف فایل‌های اضافی

پس از اینکه نسخه نهایی بدون وابستگی به فایل‌های قدیمی کار کرد:

فهرست فایل‌های غیرضروری را تهیه کن.

dependency آن‌ها را بررسی کن.

فقط فایل‌های واقعاً غیرضروری را حذف کن.

دوباره سایت و داده را تست کن.

git status را بررسی کن.

به‌ویژه بررسی کن که این موارد حذف شده باشند، مگر اینکه دلیل واقعی برای نگهداری وجود داشته باشد:

scripts/
exports/
SQLite files
old generated JSON files
temporary reports
debug files
data loader examples
duplicate prompt files
old HTML prototypes

پوشه final را حذف نکن.

۲۰. معیار پذیرش

کار زمانی کامل است که:

مسیر صحیح /home/hadi/gyn/hadi/gynqb استفاده شده باشد.

دقیقاً ۱٬۶۰۸ سؤال یکتا در خروجی وجود داشته باشد.

تمام فیلدهای اصلی حفظ شده باشند.

هیچ سؤال ناقص حذف نشده باشد.

docs/questions.json معتبر باشد.

سایت اصلی تمام اطلاعات سؤال را نمایش دهد.

پاسخ فقط پس از انتخاب کاربر نمایش داده شود.

فیلترها و جستجو کار کنند.

پیشرفت کاربر در localStorage ذخیره شود.

پنل مدیریت تمام رکوردها را ببیند.

پنل به‌صورت پیش‌فرض needs_review را نمایش دهد.

تمام فیلدهای رکورد قابل ویرایش باشند.

draft مدیریت در localStorage ذخیره شود.

خروجی JSON و JSONL معتبر تولید شود.

محدودیت ذخیره مستقیم روی GitHub به‌درستی اعلام شده باشد.

هیچ backend یا build script دائمی ایجاد نشده باشد.

ساختار repo ساده شده باشد.

پوشه‌های قدیمی غیرضروری حذف شده باشند.

CNAME در صورت وجود حفظ شده باشد.

سایت در موبایل و دسکتاپ قابل استفاده باشد.

متن رابط فارسی بدون غلط املایی واضح باشد.

امنیت پایه DOM رعایت شده باشد.

README کامل شده باشد.

تست محلی موفق باشد.

وضعیت Git نهایی روشن و قابل commit باشد.

۲۱. Commit و push

ابتدا تمام تست‌ها را کامل کن.

سپس فایل‌های تغییرکرده را دقیق بررسی کن:

git status --short
git diff --stat
git diff

فقط فایل‌های لازم را stage کن.

پیام commit پیشنهادی:

Rebuild static question bank with browser admin editor

سپس:

git push origin main

اگر دسترسی push وجود ندارد:

کار را متوقف نکن.

فایل‌ها را کامل کن.

commit محلی را در صورت امکان انجام بده.

دستور دقیق push را در گزارش نهایی ارائه بده.

۲۲. گزارش نهایی

در پایان یک گزارش کوتاه و دقیق با این ساختار ارائه بده:

خلاصه تغییرات
- ...

ساختار نهایی ریپوزیتوری
- ...

داده
- تعداد کل:
- شناسه یکتا:
- complete:
- needs_review:
- پاسخ خالی:
- گزینه ناقص:
- توضیح خالی:
- رفرنس خالی:
- فصل خالی:

وب‌سایت
- صفحه اصلی:
- پنل مدیریت:
- localStorage:
- import/export:
- responsive:
- accessibility:

فایل‌های حذف‌شده
- ...

فایل‌های حفظ‌شده و دلیل
- ...

تست‌ها
- JSON validation:
- JSONL validation:
- local HTTP:
- UI smoke test:
- admin export test:
- XSS safety check:

Git
- branch:
- commit:
- push:
- GitHub Pages:
- CNAME:

اگر موردی حل نشده است، آن را صریح و بدون ادعای موفقیت گزارش کن.

دستور اجرایی نهایی

اکنون repo را در مسیر /home/hadi/gyn/hadi/gynqb بررسی کن، منبع نهایی ۱٬۶۰۸ سؤال داخل پوشه final را شناسایی و اعتبارسنجی کن، تمام فایل‌ها و پوشه‌های غیرضروری را پس از بررسی حذف کن، docs/questions.json را بدون اسکریپت دائمی بساز، سایت اصلی docs/index.html و پنل مدیریت docs/admin.html را با Vanilla JavaScript پیاده‌سازی کن، تمام تست‌ها را اجرا کن و نسخه نهایی را برای GitHub Pages آماده ساز.

هیچ داده علمی را حدس نزن، هیچ سؤال ناقص را حذف نکن، هیچ پاسخ یا رفرنس جدید از خودت تولید نکن و محدودیت static بودن پنل مدیریت را پنهان نکن.
